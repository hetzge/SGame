package de.hetzge.sgame.render;

import java.util.HashMap;
import java.util.Map;

public class RenderablePool {

	private final Map<IF_RenderableKey, Renderable<?>> renderables = new HashMap<>();

	public RenderablePool() {
	}

	public void registerRenderable(IF_RenderableKey renderableKey, Renderable<?> renderable) {
		this.renderables.put(renderableKey, renderable);
	}

	public Renderable<?> getRenderable(IF_RenderableKey renderableKey) {
		return this.renderables.get(renderableKey);
	}

}
