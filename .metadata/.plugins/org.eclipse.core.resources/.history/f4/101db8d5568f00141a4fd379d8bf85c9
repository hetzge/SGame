package de.hetzge.sgame.common.renderable;

import de.hetzge.sgame.common.Orientation;

public class RenderableKey {
	public IF_AnimationKey animationKey;
	public IF_EntityKey entityKey;
	public Orientation orientation;

	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((animationKey == null) ? 0 : animationKey.hashCode());
		result = prime * result + ((entityKey == null) ? 0 : entityKey.hashCode());
		result = prime * result + ((orientation == null) ? 0 : orientation.hashCode());
		return result;
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		RenderableKey other = (RenderableKey) obj;
		if (animationKey == null) {
			if (other.animationKey != null)
				return false;
		} else if (!animationKey.equals(other.animationKey))
			return false;
		if (entityKey == null) {
			if (other.entityKey != null)
				return false;
		} else if (!entityKey.equals(other.entityKey))
			return false;
		if (orientation != other.orientation)
			return false;
		return true;
	}

}
